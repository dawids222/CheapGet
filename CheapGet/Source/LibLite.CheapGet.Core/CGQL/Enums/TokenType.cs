namespace LibLite.CheapGet.Core.CGQL.Enums
{
    public enum TokenType
    {
        SELECT,
        FROM,
        FILTER,
        SORT,
        TAKE,
        SORT_DIRECTION,
        COMPARISON,
        TEXT,
        FLOATING,
        INTEGER,
        LOAD,
        CLS,
        EXIT,
        EOF,
        UNRECOGNISED,
    }
}
