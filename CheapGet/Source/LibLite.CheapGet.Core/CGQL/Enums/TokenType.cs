namespace LibLite.CheapGet.Core.CGQL.Enums
{
    public enum TokenType
    {
        SELECT,
        FROM,
        FILTER,
        SORT,
        TAKE,
        SORT_DIRECTION,
        COMPARISON,
        TEXT,
        FLOATING,
        INTEGER,
        WISHLIST,
        WISH,
        MAX,
        LOAD,
        COMBINE,
        CLS,
        EXIT,
        EOF,
        UNRECOGNISED,
    }
}
